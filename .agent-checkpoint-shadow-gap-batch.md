# Shadow correctness batch checkpoint

Current task: resume and complete the six-task shadow correctness/gap batch.

Already complete (commits `91866eb` and `5b71f78`): awaited core shadow chains,
as-of transient-water replay, FatWeight suffix/lifecycle fixes, and the initial
canonical resistance-exposure contract.

Current work completed: glycogen partial-coverage and gap-bridge semantics.
`ExperimentalGlycogenStateV1` now exposes complete/partial/modeled-gap-bridge
coverage; unknown daily net change and glycogen water are null rather than a
fake available zero, while the prior relative debt remains a latent bridge.
Focused glycogen test: 11 passed. Typecheck: passed.

Current work in progress: as-of audit. Strength active-energy body-mass fallback
is now bounded by the session's local calendar date, so a future scale weight
cannot alter a historical session estimate. Focused energy/transient/glycogen:
36 passed; typecheck passed. This needs a PostgreSQL regression fixture before
being counted as complete.

Pass A continuation (uncommitted): Stepper glycogen demand no longer reads a
latest production glycogen row; the daily relative-debt state remains the only
shared cap. Muscle result lineage now labels training-only SM rows as
`diagnostic-training-contribution` and cessation rows as
`authoritative-unified-relative-muscle-state`; recomposition has a regression
accepting the cessation-aware state. Focused muscle/stepper tests: 47 passed;
typecheck passed. Still required: actual DB fixtures and deterministic suffix
rebuild orchestration for the authoritative muscle row.

Latest uncommitted Pass A work: added
`rebuildAuthoritativeRelativeMuscleTrajectory`, which replays diagnostic
training input and the authoritative cessation-aware daily state from a
historical changed session date through the latest durable health day. Training
completion and delayed matching invoke it after writing session shadows.
Focused training/cessation: 28 passed; typecheck passed.

Still required to finish Pass A: PostgreSQL future-data fixtures (weight,
glycogen/V7, measurement/activity), actual DB backfill/full-vs-suffix/shuffled
trajectory tests, and the final remaining as-of query audit report/fixes.

Additional uncommitted as-of fix: the legacy diagnostic
`ExperimentalGlycogenAssociatedWaterShadow` now bounds its V7 predecessor by
`date <= targetDate`; the matching repletion lookup was already bounded.

Remaining acceptance areas after this slice:
- shared multi-source daily coverage/gap contract and all scenario tests;
- one persisted authoritative relative-muscle trajectory;
- audit/fix every remaining as-of fallback;
- measurement/sleep/calibration dedupe and validated-accuracy separation;
- fat uncertainty discordance and post-gap recovery;
- full cross-system integration suite.

Current HEAD before this uncommitted as-of slice: `e442d196f66660f049ee493607a05ad9032647af`.

Latest continuation (uncommitted on top of `f6082de`):
- added the pure `experimental-data-gap-context-v1` contract, distinguishing
  observed, device/model estimates, modeled bridges, carried computational
  state, partial/unresolved/missing coverage; short/large/extended gap and
  recovery semantics are deterministic and never mutate raw sources;
- health sync now processes multi-day responses chronologically and replays the
  authoritative muscle suffix once from the earliest changed day;
- chronic sleep trajectory deduplicates one calendar night and resets an
  observed-low-sleep streak across calendar gaps, so duplicate rows or weeks
  apart cannot manufacture chronic evidence.

Verification after latest continuation:
- focused gap/sleep/cessation/fat tests: 4 files, 27 tests passed;
- PostgreSQL integration: physiology persistence + fat shadow, 12 tests passed
  (the Docker client itself was permission-denied, but the existing local
  PostgreSQL test database was reachable);
- full unit: 224 files passed, 2 skipped; 2259 tests passed, 11 skipped;
- typecheck, manifest tests, production build and `git diff --check`: passed;
- lint: passed with two pre-existing manifest unused-variable warnings.

Still not implemented/proven for the final user contract: persisted shared gap
context; DB-backed fixtures for each future-source category and muscle suffix
backfill/shuffle; runtime calibration dedupe distinction; and all ten
cross-system E2E scenarios. Do not claim the whole batch complete.

Latest uncommitted continuation:
- measurement validation now deduplicates replayed source/timestamp/method/
  device/site identities before counting repeated observations;
- personal energy calibration now separates eligible compatible coverage from
  validated accuracy: repeated Garmin/HR metadata has `eligibleForCalibration`
  but never narrows uncertainty or claims accuracy;
- gap context now owns monotonic short/large/extended-gap uncertainty widening
  and gradual valid-coverage recovery semantics.

Focused latest tests: measurement/calibration/gap, 18 passed; typecheck and
diff check passed. The five major final deliverables remain incomplete because
the new generic contract is not yet persisted through every shadow service and
the requested DB/E2E fixture matrix has not been implemented.

Gap runtime wiring started: the authoritative experimental glycogen daily
shadow now derives source-specific nutrition/training/activity coverage, stores
the shared gap context in its result/features, and carries only its previous
gap context across chronological dates. Focused glycogen test (11) and
typecheck passed after this wiring.

Further gap wiring (uncommitted): authoritative cessation/detraining and
fat-weight uncertainty persistence now store source-relevant shared gap
contexts with their own predecessor context; no irrelevant sleep/HR source is
introduced. Added `experimental-shadow-gap-e2e.test.ts`, ten cross-transition
scenarios exercising gap context with actual glycogen, cessation, transient
water, and fat uncertainty transitions. All ten pass; typecheck passes.

Critical orchestration correction: health sync's authoritative muscle suffix
rebuild was initially placed inside the per-date loop. It is now correctly run
once after the full chronological batch. Health-sync + E2E tests (24) and
typecheck passed.

## Block A runtime/persistence audit inventory (complete)

Classification key: A=authoritative longitudinal; B=diagnostic/context;
C=stateless/source-local; D=legacy compatibility (must not compete).

| Module | Class | Gap sources | As-of |
| --- | --- | --- | --- |
| ExperimentalSkeletalMuscleDelta | B (diagnostic training contribution) | training feed/diary via exposure classifier | prior `date < target` SAFE |
| ExperimentalCessationDetraining | A (authoritative unified relative muscle) | training only; shared gapContext persisted | prior `date < target` SAFE |
| rebuildAuthoritativeRelativeMuscleTrajectory | A orchestration | suffix end = latest health `>= from` INTENTIONAL | calendar replay SAFE |
| Retraining Identification | B label/context | n/a (pure) | pure SAFE |
| FFM Retention | B | prior fat `date < target` | SAFE |
| Body Recomposition | B consumer of cessation relative state | n/a (pure) | pure SAFE |
| Transient Exercise Water | A compartment (elapsed-time bridge) | training impulses; daysElapsed gap aging | predecessor `< targetDate` SAFE |
| FatWeightShadowV1 | A mean trajectory | weight/BF%/energy | predecessor `< from` SAFE |
| FatWeight Uncertainty | A + shared gapContext | nutrition/weight/bodyComposition only | predecessor SAFE |
| Strength Active Energy | C session-local | body mass as-of `date <= session` | FIXED SAFE |
| Stepper Active Energy | C workout-local | workout-day weight | CURRENT-DATE SAFE |
| Strength Glycogen Demand | B diagnostic | availableGlycogenKg forced null | SAFE |
| Stepper Glycogen Demand | B diagnostic | availableGlycogenKg forced null; snapshots bracket = INTENTIONAL RETROSPECTIVE | SAFE |
| Glycogen State | A + shared gapContext | nutrition/training/activity only | prior `< from` SAFE |
| Glycogen Associated Water | D legacy diagnostic | V7 `date <= target` FIXED | SAFE |
| Glycogen Repletion | D legacy diagnostic | production glycogen `date <= target` | SAFE |
| Local Hypertrophy | B | session-day local | SAFE |
| Measurement Validation | C pure | dedupe before confidence | pure SAFE |
| Chronic Sleep Context | B pure | night dedupe; gap resets streak | pure SAFE |
| Age Maintenance Uncertainty | B pure | n/a | pure SAFE |
| Personal Energy Calibration | C pure | eligibility ≠ validated accuracy | pure SAFE |
| Health sync orchestration | A | chronological days; muscle suffix ONCE after batch | SAFE |
| Training completion/matching | A | awaited shadows + suffix rebuild | SAFE |

Historical as-of pattern scan completed across experimental/shadow runtime.
No remaining UNSAFE future-leak reads in batch scope.

## Block B PostgreSQL fixtures A–I (complete)

`tests/integration/shadow-gap-future-asof-matrix.integration.test.ts`
and `tests/health-sync-authoritative-muscle-once.test.ts`.

Final gates (this completion pass):
- fixtures A–I: 9/9 PASS (E sleep/calibration/measurement subcases N/A with code evidence)
- E2E 10/10; focused gap/uncertainty/muscle/glycogen/water green
- full unit 2271 passed / 11 skipped; integration 79 passed
- typecheck, lint touched files, prisma validate, production build, git diff --check: passed
- manifest: GREEN 38 / EXPERIMENTAL 41 / BLOCKED 0 / REJECTED 4

WHOLE BATCH: COMPLETE.
